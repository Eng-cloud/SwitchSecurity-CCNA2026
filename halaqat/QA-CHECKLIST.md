# قائمة فحص التسليم — Design QA (المواصفات §79 · §81)

آخر تشغيل: `npm test` (56 ✓) + `npx playwright test` (82 ✓ على سطح المكتب والجوال) + `npm run build` ✓

## اجتياز المرحلة (§79)

| البند | الحالة | كيف تم التحقق |
| --- | --- | --- |
| الصفحات تعمل | ✓ | `smoke.spec.js` يفتح كل مسارات الأدوار الخمسة ويتأكد من عنوان الصفحة |
| الأزرار تعمل | ✓ | رحلات الأدوار تنفّذ الإجراءات فعليًا (حضور، ملاحظة، جلسة، اختبار، حفظ) |
| Navigation يعمل | ✓ | قائمة جانبية + شريط سفلي + درج الجوال + مسار التنقل |
| Browser Back يعمل | ✓ | `behavior.spec.js` + رحلة المعلم (رجوع المتصفح بعد فتح الطالب) |
| In-App Back يعمل | ✓ | زر "رجوع" في ملف الطالب |
| Forms تعمل | ✓ | الدخول، التحقق، التسجيل، الملاحظة، الجلسة، الأهداف، الإعدادات |
| Loading موجود | ✓ | `DataState` + هياكل تحميل + حالات الأزرار |
| Error موجود | ✓ | `ErrorState` مع إعادة المحاولة + أخطاء الحقول + حاجز أخطاء عام |
| Empty موجود | ✓ | قوائم فارغة ونتائج بحث فارغة (مختبَرة) |
| Success موجود | ✓ | Toast + حالة نجاح على الأزرار + أثر مرئي في الصفحة |
| RTL | ✓ | `dir="rtl"` على المستند، وخصائص منطقية في كل CSS |
| Mobile | ✓ | مشروع `mobile` في Playwright يمرّ كاملًا |
| Desktop | ✓ | مشروع `desktop` يمرّ كاملًا |
| Dark / Light / System | ✓ | مختبَر مع الحفظ والاستجابة لتغيّر تفضيل النظام |
| Accessibility | ✓ | 10 اختبارات وصول مخصصة |
| لا أخطاء Console | ✓ | كل اختبار E2E يفشل عند أي خطأ Console |

## Design QA (§81)

| البند | الحالة | ملاحظة |
| --- | --- | --- |
| لا صفحة تشبه الجريدة | ✓ | حد أقصى للنص (`measure`)، ولا فقرات طويلة في اللوحات |
| لا Dashboard مزدحم | ✓ | 4 مؤشرات + إجراء أساسي واحد + نشاط أخير |
| لا نص زائد | ✓ | رسائل قصيرة و"عرض التفاصيل" للتفاصيل |
| المعلومات حسب الأولوية | ✓ | ملخص ← إجراء ← مؤشرات ← نشاط ← تفاصيل |
| التفاصيل عند الطلب | ✓ | تبويبات، طيّ، نوافذ، أدراج، صفحات تفاصيل |
| Buttons واضحة | ✓ | نص صريح لكل زر ولا أيقونة بلا وصف |
| Forms واضحة | ✓ | Label دائم (لا Placeholder بديلًا)، تلميح، خطأ مرتبط |
| RTL صحيح | ✓ | لا قلب آلي — تصميم عربي أصلي |
| العربية صحيحة | ✓ | كل النصوص من `i18n/ar.js` |
| Light / Dark / System | ✓ | إعادة ضبط كاملة للوضع الداكن وليست قلبًا للألوان |
| Mobile / Tablet / Desktop | ✓ | إعادة تنظيم لا تصغير (جداول → بطاقات، قائمة → شريط سفلي) |
| Keyboard يعمل | ✓ | Tab / Shift+Tab / Enter / Space / Escape / الأسهم |
| Screen Reader تمت مراجعته | ✓ | معالم بأسماء مميزة، إعلان المسار، Live Regions، أوصاف الأزرار |
| Reduced Motion يعمل | ✓ | من النظام أو من إعدادات المنصة |
| Contrast مناسب | ✓ | وضع تباين مرتفع منفصل للفاتح والداكن |
| لا Overflow | ✓ | مفحوص برمجيًا في كل الأدوار على الجوال وسطح المكتب |
| لا Routes مكسورة | ✓ | كل مسار مختبَر + صفحة 404 |
| لا أخطاء Console معروفة | ✓ | مراقبة في كل اختبار |

## عيوب وُجدت أثناء الاختبار وأُصلحت

1. **جلسة المعلم بلا معرّف حلقة** → صفحتا الحلقة والجلسات تظهران فارغتين. (أُصلح في `authService` + اختبار انحدار)
2. **استعادة موضع التمرير لا تعمل** → كان الموضع يُحفظ بعد أن يصفّره المتصفح. (أُعيدت كتابة `ScrollRestoration` لتسجّل أثناء التمرير مع إعادة محاولة بعد وصول البيانات)
3. **`formatNumber(null)` تعرض `0`** بدل شرطة — رقم مضلل في التقارير. (أُصلح + اختبار)
4. **حصر التركيز يتجاهل العناصر** عند غياب التخطيط، والنوافذ بلا هدف تركيز احتياطي. (أُصلح في `useFocusTrap` + `tabIndex` على النوافذ)
5. **معلمان للتنقل بنفس الاسم** (`التنقل الرئيسي`) — التباس لقارئ الشاشة. (أسماء مميزة الآن)

## حدود صريحة

- لا يوجد Backend: كل شيء محاكاة في المتصفح.
- تحليل التسميع Mock حتمي وليس ذكاءً اصطناعيًا — مُعلَّم داخل الواجهة.
- نص السور غير المتضمَّنة غير معروض (إشعار صريح) بدل توليد نص قرآني.
- الأدوار تجربة واجهة ولا تمثل تفويضًا حقيقيًا.
